server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegisrty: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:4{server.port}/eureka/
